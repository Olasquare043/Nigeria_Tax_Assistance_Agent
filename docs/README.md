All the PDFs File will be place inside this folder for our ingestion and build index


The engine will ingest every `.pdf` file in this folder.

Tip: we should avoid double extensions like `.pdf.pdf`.

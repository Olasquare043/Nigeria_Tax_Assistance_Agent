# Frontend (React + Tailwind)

Suggestion: May be we should create `frontend/.env` to set backend URL:

```bash
VITE_API_BASE=http://localhost:8000
```

Run:
```bash
npm install
npm run dev
```
<!-- U guys know other files to add for the frontend part -->